import unittest 

# python -m unittest get_test.py
class Test(unittest.Testcase):
	
	def test_get_bias(self, fasta):
		result = ""
		self.assertequal(result, get_bias(fasta)
	
	def test_get_data(self, fasta):
		result = ""
		self.assertequal(result, get_bias(fasta)
	
	def test_gethegs(self, fasta):
		result = ""
		self.assertequal(result, get_bias(fasta)
	
#makes the running test easier
if __name__ == '__main__':
	unittest.main()